import React from 'react'

export default (props) => {
  const style = {
    position: 'relative',
    margin: '50px auto',
    width: '600px',
    height: '600px',
    border: '2px solid #000'
  }

  return <div style={style}></div>
}
